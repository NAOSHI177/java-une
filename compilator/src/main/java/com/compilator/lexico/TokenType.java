package com.compilator.lexico;

public enum TokenType {
    KEYWORD_INT,
    KEYWORD_PRINT,
    IDENTIFIER,
    NUMBER,
    ASSIGN,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    LPAREN,
    RPAREN,
    SEMICOLON,
    EOF
}
